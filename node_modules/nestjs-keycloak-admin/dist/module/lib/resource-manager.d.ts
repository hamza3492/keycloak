import { Resource } from '../uma/resource';
import { KeycloakService } from '../service';
import { ResourceQuery } from '../@types/resource';
export declare class ResourceManager {
    private readonly requestManager;
    constructor(client: KeycloakService, endpoint: string);
    create(resource: Resource): Promise<Resource>;
    update(resource: Resource): Promise<Resource>;
    delete(resource: Resource): Promise<void>;
    findById(id: string): Promise<Resource | null>;
    findAll(params: ResourceQuery): Promise<Resource[] | any>;
    findByAttribute(key: string, attribute: string): Promise<string[]>;
}
//# sourceMappingURL=resource-manager.d.ts.map